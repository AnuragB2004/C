#include<stdio.h>
//WAP to calculate the area of a triangle
int main(){
float base, height;
float area = 0.5 * base * height;
    printf("Enter base: \n");
    scanf("%f", &base);
    printf("Enter height: \n");
    scanf("%f", &base);

    printf("Area of triangle is: %f", area);
    return 0;
}